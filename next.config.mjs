/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: '/relong-cloud',
    // assetPrefix: '/relong-cloud/',
    // output: 'export',
    // distDir: 'build'
};

export default nextConfig;
